// Nombre del alumno: DANIEL ROLDAN SERRANO
// Usuario del Juez: DG25

#include <iostream>
#include <iomanip>
#include <fstream>
#include <vector>

using namespace std;

bool resuelveCaso() {
    int num_obj, presupuesto;
    cin >> num_obj >> presupuesto;

    if (!std::cin)
        return false;


    return true;
}

int main() {
    while (resuelveCaso());
    return 0;
}